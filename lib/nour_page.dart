import 'package:flutter/material.dart';

class NourPage extends StatelessWidget {
  const NourPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Column(children: [Text('nour screen')])),
    );
  }
}
