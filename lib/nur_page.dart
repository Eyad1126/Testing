import 'package:flutter/material.dart';

class NurPage extends StatelessWidget {
  const NurPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}